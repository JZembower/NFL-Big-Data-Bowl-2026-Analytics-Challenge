# Core data science stack
pandas>=2.0
numpy>=1.24

# Visualization
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# Machine Learning – classic
scikit-learn>=1.3
xgboost>=2.0

# Imbalanced classes (used in 04_modeling_completions)
imbalanced-learn>=0.11

# Deep Learning (used in 03_modeling_routes)
torch>=2.0
torchvision>=0.15        # usually installed automatically with torch
torchaudio>=2.0          # optional, but safe to include

# Progress bars & utilities
tqdm>=4.65
scipy>=1.10

# Optional but very useful
pathlib
pickle5                  # fallback for older pickle protocols (not strictly required)
gc                       # built-in, listed for clarity

# For saving Parquet files efficiently (highly recommended)
pyarrow>=12.0
fastparquet>=2023.10     # alternative engine, one of them is enough

# Jupyter / Notebook extras (if you run as .ipynb)
jupyterlab>=4.0
ipywidgets>=8.0